# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# This project uses pnpm — pnpm-lock.yaml is the single source of truth.
# Ignore other package managers' lockfiles so an accidental `npm`/`yarn`
# install can't reintroduce a competing lockfile (and break CI's frozen check).
package-lock.json
yarn.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env overrides. `.env.development` / `.env.production` stay tracked on
# purpose — they only carry public VITE_* config baked into the bundle.
.env.local
.env.*.local

# testing
/coverage

# vercel
.vercel
